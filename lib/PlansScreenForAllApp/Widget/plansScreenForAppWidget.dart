import 'package:flutter/material.dart';

class PlansScreenForAppWidget extends StatelessWidget {
  const PlansScreenForAppWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("data"),
    );
  }
}
