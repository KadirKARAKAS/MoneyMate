import 'package:flutter/material.dart';

class NewPlansCreatePageWidget extends StatelessWidget {
  const NewPlansCreatePageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
